<h3><%= name %></h3>

<% if(!_.isUndefined(arguments[0].media)) { %>
  <%= KR.MediaCarousel.CreateMediaListMarkup(media) %>
<% } %>


<% if(!_.isUndefined(arguments[0].description)) { %>
<p><%= description %></p>
<% } %>


<table class="sidebar-table">
    <tr>
      <td>Type</td>
      <td><%= loccatlabel %></td>
    </tr>
    <% if(!_.isUndefined(arguments[0].locartlabel)) { %>
    <tr>
      <td>Lokalitetsart</td>
      <td><%= locartlabel  %></td>
    </tr>
    <% } %>
    <% if(!_.isUndefined(arguments[0].orglabel)) { %>
    <tr>
      <td>Ansvarlig organisasjon</td>
      <td><%= orglabel  %></td>
    </tr>
    <% } %>
</table>

<ul class="list-unstyled distance">
    <li>
	    <a href="<%= url %>"  target="_blank" class="sidebar-link" >
			<i class="fa fa-external-link-square"></i> Vis i Riksantikvarens Kulturminnes&oslash;k
		</a>
    </li>
</ul>



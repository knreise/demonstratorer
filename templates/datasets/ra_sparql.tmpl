<h3><%= name %></h3>

<% if (img) { %>
<img class="fullwidth img-thumbnail" src="<%= img %>" />
<% } %>

<p><%= description %></p>

<table class="sidebar-table">
    <tr>
      <td>Type</td>
      <td><%= loccatlabel %></td>
    </tr>
</table>

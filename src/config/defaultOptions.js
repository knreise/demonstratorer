/*Default options for map*/
const DEFAULT_OPTIONS = {
    geomFilter: false,
    showGeom: false,
    loactionHash: true,
    featureHash: true,
    restrictMap: false
};
export default DEFAULT_OPTIONS;